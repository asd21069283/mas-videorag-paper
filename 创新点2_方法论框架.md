# 创新点2 方法论框架：Q-driven 客体级关键证据定位 → 裁剪/生成统一链路
## (Q-driven Object-Level Evidence Localization with Unified Crop-or-Generate)

> 主线一句话：把"问题驱动选帧"从**帧级**下沉到 **SVO-客体级**，用客体 `o` 同时驱动**选帧 + 选框 + 裁剪/生成决策**，并在检索/裁剪失败时**退化为生成新图**。
> 整理：方法论调研 agent（已联网核对 AKS / GROVE / Video-RAG / M2RAG），日期 2026-06-25。可直接作为论文 Method 章节初稿。

---

## 0. 符号约定（Notation）

| 记号 | 含义 |
|---|---|
| `V = {F_1,…,F_N}` | 输入视频的帧序列（N 帧） |
| `Q` | 用户文字指令（query/instruction） |
| `T = {(s, v, o)_k}` | 从 `Q` 抽出的 SVO 三元组集合（subject–verb–object） |
| `o ∈ O_Q` | 目标客体（target object），`O_Q` 为指令涉及的客体集合 |
| `s(o, F)` | 客体条件下的帧相关性（object-conditioned relevance） |
| `c(I)` | 选帧子集 `I ⊆ V` 的时序覆盖度（coverage） |
| `I*` | 最优关键帧子集，`|I*| ≤ B`（帧预算 budget B） |
| `b = (F, o, box, ρ)` | 一条客体级证据：帧、客体、检测框、grounding 置信度 ρ |
| `ρ(o, F)` | Grounding-DINO/APE 对客体 `o` 在帧 `F` 的检测置信度 |
| `τ_g` | grounding 置信度阈值（触发裁剪 vs 生成） |
| `τ_r` | 检索相似度阈值（共享 RAG 命中判据） |
| `ψ(·)` | 文本/跨模态嵌入（BGE-M3），用于 caption 去冗余与检索对齐 |

---

## 1. 总体管线（5 阶段）

```
指令 Q + 视频 V
   │
   ▼
① 指令解析抽 SVO ──► ② Q-driven 关键帧选择 ──► ③ 客体级 grounding 定位
                                                          │
                                                          ▼
                                            ④ 裁剪 / 生成 决策（闭环）
                                                          │
                                                          ▼
                                            ⑤ 图文混排输出（interleaved）
```

### ① 指令解析 · 抽 SVO
- 输入：用户指令 `Q`。
- 处理：POS tagging + dependency parse（借鉴 GROVE 的 object 短语抽取，做"反向"——从指令里抽 object）抽出 `T={(s,v,o)}`；客体 `o` 保留字面词 + LLM 同义扩展（可选）。
- 输出：目标客体集 `O_Q`、谓词集。
- 训练状态：免训练。
- ⚠️ GROVE 原文是把 caption 里的 object grounding 到 box；本框架把"POS 抽客体"迁移到**指令端**，论文中标注为 "inspired by"。

### ② Q-driven 关键帧选择
- 输入：`V`、`O_Q`。
- 处理：先用 BGE-M3 caption 余弦相似去冗余（VideoEspresso 思路）把 N 帧压到 N'；再把 AKS 的 `s(Q,F)` 升级为客体条件 `s(o,F)`，用 recursive judge-and-split 在帧预算 B 下解 relevance+coverage 折中。
- 输出：关键帧子集 `I*`（每帧附触发它的客体标签）。
- 训练状态：免训练（沿用 AKS plug-and-play）。

### ③ 客体级 grounding 定位
- 输入：`I*`、`O_Q`。
- 处理：对每个 `(F∈I*, o∈O_Q)`，用 Grounding-DINO（开放词表检测）或 Video-RAG 的 APE 输出 box 与置信度 `ρ(o,F)`。
- 输出：候选证据集 `B={(F,o,box,ρ)}`。
- 训练状态：免训练。

### ④ 裁剪/生成决策（核心闭环）
- 输入：`B`、共享 RAG 库、客体 `o`。
- 处理：见 §3 判据。命中且置信度够→裁剪现有帧；否则→退化生成新图（超越 M2RAG 的"只选不画"）。
- 输出：每个配图位点一张图（裁剪/生成）+ 来源标记。

### ⑤ 图文混排输出
- 输入：Understand Agent 文字答案 + ④的图集。
- 处理：Generation Agent 决定插图位点与图注，对齐到答案中提到 `o` 的句子。
- 输出：图文交织最终回答。

---

## 2. ASCII 架构框图

```
                              ┌───────────────────────────────────────────────┐
            用户输入            │                 UNDERSTAND AGENT               │
        ┌──────────────┐       │                                               │
        │  Q (指令)    │──────►│  ① SVO 抽取 (POS/parse, GROVE-style)          │
        │  V (视频)    │──┐    │        │  O_Q={o_k}, 谓词                       │
        └──────────────┘  │    │        ▼                                       │
                          │    │  ② Q-driven 选帧  s(o,F)+c(I)  (AKS 升级)       │
                          └───►│   ▲    │  I* (关键帧+客体标签)                  │
                  BGE-M3 去冗余 │   │    ▼                                       │
                  (VideoEspresso│   │  ③ 客体级 grounding (G-DINO / APE)         │
                   caption 余弦)│   │    │  B={(F,o,box,ρ)}                       │
                               │   │    ▼                                       │
                               │   │  ┌──── 文字答案 (因果/动作推理) ───┐        │
                               │   │  └──────────────┬──────────────────┘        │
                               └───┼─────────────────┼──────────── 反馈①         │
                                   │                 │  (ρ<τ_g ⇒ 回阶段②重选帧) │
                                   └─────────────────┼─────────────────────────┘
                                                     ▼
                ┌────────────────────────────────────────────────────────────┐
                │                      SHARED  RAG                            │
                │  视觉记忆: 帧嵌入 / 已裁剪图 / 检测库      文本: OCR·ASR·caption │
                │  检索: ψ(o)·ψ(cand) ≥ τ_r ?                                  │
                └───────────────┬─────────────────────────┬───────────────────┘
                                │ 命中(crop 候选)          │ 未命中
                                ▼                          ▼
                ┌────────────────────────────────────────────────────────────┐
                │                    GENERATION AGENT                         │
                │  ④ 决策:  ρ≥τ_g & 检索命中 ──► CROP(裁剪现有帧, box 扩边)     │
                │           否则            ──► GENERATE(T2I/编辑, 以 o 为条件)│
                │                              └─ 反馈②: 生成图回写 Shared RAG │
                │  ⑤ 图文混排: 图↔提到 o 的句子对齐, 生成图注                  │
                └───────────────────────────┬────────────────────────────────┘
                                            ▼
                                  图 + 文字 (interleaved 输出)
```

**两条反馈回路**：
- 反馈①（grounding→选帧）：若客体 `o` 在 `I*` 内所有帧 `ρ<τ_g`，回②扩大帧预算 `B` 或松弛去冗余阈值，重选含 `o` 的帧。
- 反馈②（生成→RAG）：新生成图写回 Shared RAG，供后续位点复用，避免重复生成。

---

## 3. 关键公式 / 记号

### 3.1 帧相关性：从 `s(Q,F)` 到 `s(o,F)`
```
s(o, F) = sim( ψ_img(F) , ψ_txt(o) )          # 客体级相关性 (BGE-M3 跨模态/CLIP-like)
```
多客体聚合（权重 `w_o` 默认均匀，或按 SVO 中 object 频次/位置加权）：
```
S(O_Q, F) = Σ_{o∈O_Q} w_o · s(o, F) ,   Σ w_o = 1
```

### 3.2 选帧目标函数（relevance + coverage，客体条件版）
```
I* = argmax_{I⊆V, |I|≤B}  [  Σ_{F∈I} S(O_Q, F)  +  λ · c(I)  ]
      c(I) = − Σ_{j=1}^{M} | n_j(I) − |I|/M |     # M 个时间分箱的不均衡负惩罚
      n_j(I) = 落入第 j 个时间箱的选帧数;   λ ≥ 0 平衡相关性与覆盖
```
求解：AKS 的 recursive judge-and-split（按相关性增益阈值递归二分，training-free 近似）。
⚠️ AKS 未公开 `c(I)` 精确闭式；上式是与其"保证时序覆盖"自洽的实现，论文标注为"我们对 AKS coverage 的具体化"。

### 3.3 裁剪 vs 生成判据
```
ρ*(o) = max_{F∈I*}  ρ(o, F)                   # 最高检测置信度
r*(o)  = max_{cand∈RAG}  sim(ψ(o), ψ(cand))    # 最高检索相似度

           ┌  CROP(o)      若  ρ*(o) ≥ τ_g  且  r*(o) ≥ τ_r     (证据充分 → 裁剪现有帧)
decide(o)= ┤
           └  GENERATE(o)  否则                                 (退化为生成新图)
```
- CROP：取 `argmax_F ρ(o,F)` 的 box，按比例 α 扩边裁剪。
- GENERATE：以 `o`（及 SVO 上下文/场景）为条件做 T2I 或参考图编辑；产物写回 RAG。
- 阈值起点：`τ_g≈0.35`（G-DINO 常用档），`τ_r≈0.5`（BGE-M3 经验），均需验证集扫。

---

## 4. 训练与损失

| 模块 | 状态 | 说明 |
|---|---|---|
| SVO 抽取 | 免训练 | parser + LLM prompt |
| Q-driven 选帧 (AKS 升级) | 免训练 | 仅改打分条件 |
| BGE-M3 去冗余/检索对齐 | 免训练(可选 LoRA) | 领域差异大时轻量 LoRA |
| Grounding-DINO / APE | 免训练 | 现成权重 |
| Understand Agent (video-LLM) | 可选 LoRA | 强化"客体证据→因果答案" |
| Generation Agent (T2I/编辑) | 可选 LoRA | 生成风格与视频域对齐 |

可选损失（引入 LoRA 时）：
- 检索对齐 `L_ret = InfoNCE(ψ(o), ψ(正确证据))`
- grounding `L_gnd = L_box(GIoU) + L_cls`（仅微调检测器时）
- 图文一致性 `L_cons = 1 − sim(ψ_img(图), ψ_txt(对应句子))`
- 总损失 `L = L_ret + β·L_gnd + γ·L_cons`
- 取向：**主线尽量 training-free**（plug-and-play 卖点），LoRA/损失为可选增强。

---

## 5. 评测方案
- **文本/图文质量**：复用 M2RAG 的图文指标 + GPT-4o-judge。⚠️ M2RAG 摘要未逐条枚举那 8 个指标名称，落稿前对照 github.com/maziao/M2RAG 写死。
- **因果维度**：引入 CUVA 的 causation 维度，考核动作/事件因果类指令（配合 SVO 谓词 `v`）。
- **图像端**：grounding AP/IoU（定位质量）+ 人工胜率（CROP/GENERATE 产图 pairwise 偏好）。
- **端到端**：图文混排答案 GPT-4o-judge 总分 + 人工偏好。

---

## 6. 消融实验设计

| # | 消融项 | 做法 | 验证 |
|---|---|---|---|
| A1 | 去 SVO 条件 | `s(o,F)`→AKS 原始 `s(Q,F)` | SVO-客体级 vs 帧级增益 |
| A2 | 去客体级 grounding | 跳过③，整帧配图 | 客体定位对配图精度 |
| A3 | 去退化生成 | decide 永远 CROP(=M2RAG只选不画) | "生成兜底"价值 |
| A4 | 去 BGE-M3 去冗余 | 全帧进打分 | 去冗余对效率/噪声 |
| A5 | 去共享 RAG | crop 只看当前帧 | 跨帧记忆复用价值 |
| A6 | τ_g/τ_r 扫描 | 阈值网格，画 crop↔generate 比例曲线 | 决策鲁棒性 |
| A7 | 去反馈① | grounding 失败不回退重选 | 闭环反馈对召回 |
| A8 | 客体权重 w_o | 均匀 vs 频次/位置加权 | 多客体聚合策略 |

---

## 差异化（一句话 × 4）
- **vs AKS**：AKS 帧级用整 query 选帧；我们下沉到 SVO-客体级 `s(o,F)`，且选帧直接驱动选框与配图，而非只喂 video-LLM。
- **vs GROVE**：GROVE 把 caption object grounding 到视频 box（理解侧标注）；我们把"POS 抽客体"反向用到指令端，贯通到生成侧输出。
- **vs Video-RAG**：Video-RAG 用检测辅助文本增强文字理解；我们把同款检测/检索接入图像产出闭环（裁剪），失败时生成。
- **vs M2RAG**：M2RAG 只选图插文（只选不画）；我们在置信度不足时退化生成新图，补"无合适候选"的空洞。

---

## 落稿前需核实的 3 处
1. GROVE 的 POS→SVO 具体实现（摘要未公开，本框架属迁移扩展）。
2. AKS coverage `c(I)` 精确闭式（仅确认 relevance+coverage+judge-and-split 思路）。
3. M2RAG 那 8 个图文指标的确切名称/定义（对照开源仓库写死）。

> 参考：[AKS arXiv 2502.21271](https://arxiv.org/abs/2502.21271) · [GROVE arXiv 2503.10781](https://arxiv.org/abs/2503.10781) · [Video-RAG arXiv 2411.13093](https://arxiv.org/abs/2411.13093) · [M2RAG arXiv 2411.16365](https://arxiv.org/abs/2411.16365)
