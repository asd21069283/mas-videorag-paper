# 论文文献调研与进展汇总

> 整理人：Claude（替刘子轴）｜日期：2026-06-25
> 用途：可直接作为"飞书文档"交给辅导老师（每篇论文按老师要求记录：①做了什么 ②对我们的启发 ③指标如何）
> 论文方向（4 次辅导课已收敛）：**多智能体(MAS) + 多模态 RAG 的视频系统**
> 核心任务定义：**Ours = 输入「视频 + 文字」→ 输出「图片 + 文字」**

---

## 〇、一句话背景（给不熟悉的人看）

我们要做一个"会看视频、还能按你的问题把答案'画/截'出来"的 AI 系统：
- **Understand Agent（理解员）**：看懂输入视频 + 检索相关知识，定位到关键片段/关键帧。
- **Generation Agent（出图员）**：根据理解结果，把视频里的目标定位、裁剪或生成成图片，配上文字一起输出。
- 中间靠 **多模态 RAG（开卷检索）** 给两个 Agent 供料。

为什么这个题有戏——它卡在一个**没人占的模态组合缝**里：
| 已有工作 | 输入 → 输出 |
|---|---|
| VideoRAG / Video-RAG | 视频+文字 → **文字** |
| M2RAG | **图**+文字 → 图+文字 |
| ViMax | 文字/图 → **视频** |
| **Ours（我们）** | **视频+文字 → 图+文字** ← 空白 |

---

## 一、文献综述表（老师要的核心交付物）

### A 组｜核心 RAG 任务 & Baseline（决定我们站在谁肩膀上）

#### 1. VideoRAG（港大 HKUDS）— 视频+文字→文字
- 会场/链接：arXiv 2502.01549｜GitHub HKUDS/VideoRAG｜README 称 KDD'2026 接收（**待核实**）
- **做了什么**：把几百小时超长视频变成可问答知识库——建跨视频知识图谱 + 存多模态特征，再用 RAG 回答。模型栈：ASR=Distil-Whisper、帧字幕=MiniCPM-V、跨模态=ImageBind、文本向量=text-embedding-3-small、大脑=GPT-4o-mini。**单 RAG 管线，非多 Agent，只产文字。**
- **对我们的启发**：整套视频理解前端可直接搬给 Understand Agent；它"只说不画"正是我们要补的空白。论文故事可立为"从'说给你听'升级到'画给你看'"。
- **指标**：自建 LongerVideos(164 视频/134.6h/602 query)；LLM 打分胜率 vs LightRAG 57%、vs NaiveRAG 53%；Video-MME 60.2%。
- **可复现**：✅ 开源，**单张 RTX 3090/4090 24G 可跑**（重活外包给 GPT-4o-mini API）。→ **建议作"理解侧"主 baseline。**

#### 2. Video-RAG（厦大/罗切斯特）— 视频+文字→文字（免训练外挂）
- 会场/链接：**NeurIPS 2025**｜arXiv 2411.13093｜GitHub Leon1207/Video-RAG-master
- **做了什么**：免训练，把视频里看得见的隐藏信息(OCR+ASR+开放词表检测 APE)抽成文本，按问题检索后塞回现成视频大模型当外挂。
- **对我们的启发**：**它的 APE 开放词表检测能直接给出目标框(bbox)+CLIP 关键帧定位——正是我们 Generation Agent"定位/裁剪"的现成零件**；但它只把检测转成文本，没回用到生成，"闭环到图像产出"就是我们的天然贡献点。
- **指标**：LLaVA-Video-7B 在 Video-MME 67.1%→75.7%(+8.6)；72B 版 75.7% 超 Gemini-1.5-Pro。
- **可复现**：✅ 开源，门槛极低（每样本 +5 秒、+8GB 显存）。→ **建议作轻量第二对照。**

#### 3. M2RAG（北理工 BIT）— 图+文字→图+文字（输出模态离我们最近）
- 会场/链接：arXiv **2411.16365**（⚠️**老师笔记里写的 2507.09876 是另一篇 ViTCoT，引用别搞错**）｜GitHub maziao/M2RAG｜会场未定（preprint，**ACMMM 2025 无证据，待核实**）
- **做了什么**：给 query，从多模态网页检索图文，生成"图文混排"回答（自动决定哪里插哪张图）。**只"选图+插图"不"画图"**。最优方案=多阶段(生成文→决定插图→润色)。
- **对我们的启发（4 篇里复用价值最高）**：输出端"多阶段图文交错"管线几乎可照搬，**把它的图片库换成"从视频抽的关键帧候选池"即可**；它的 8 个评测指标 + GPT-4o-as-judge 脚手架可直接复用，省得自己设计 metric。它"只选不画"→ 我们"选不到好帧就生成"是增量点。
- **指标**：自建 M2RAG-Bench(仅 200 query)；多阶段 Overall 0.75 > 单阶段 0.62。
- **可复现**：✅ 开源(MIT)，数据在 HuggingFace；训练 2×A100-80G LoRA 微调 7-8B。→ **建议作"输出侧"主 baseline。**

#### 4. ViMax（港大 HKUDS）— 文字/图→视频（方向相反，借编排框架）
- 链接：arXiv 预印本｜GitHub HKUDS/ViMax(~10.6k stars)（**未见顶会接收，待核实**）
- **做了什么**：把创意/小说/剧本自动变成几分钟长视频的"AI 剧组"——5 个专职 Agent(编剧/镜头规划/图像生成/视频生成/VLM-Judge)，顺序流水线 + 依赖图并行 + 质量门控 + 失败重试。
- **对我们的启发**：**主要借它的 Agent 编排框架**（专职分工 + 质量门控 + best-of-k 的 VLM-Judge 校验组件可直接套用）。方向相反，**不能当任务 baseline**，只作 Related Work 引用。
- **指标**：视觉一致性人工胜率 vs Veo3.1 74.76%。
- **可复现**：✅ 开源，几乎不吃本地 GPU(全走 Gemini/Veo API)，门槛在 API 费。

---

### B 组｜方法类积木（CVPR/ICCV，老师 12-07 重点讲的）

#### 5. VideoEspresso（CVPR 2025 **Oral**）
- arXiv 2411.14794｜GitHub hshjerry/VideoEspresso
- **做了什么**：大规模"视频思维链(CoT)"数据集 + 先挑核心帧再两步推理的轻量框架。**关键帧去冗余**：动/静态自适应 FPS(2-4/1) → BGE-M3 抽 caption 文本特征 → 相邻帧余弦相似度超阈值就用 LIFO 删冗余 → GroundingDINO 画框。推理只用约 **2.36 帧**(对比别人 128 帧)。
- **对我们的启发**：这套"BGE-M3 相似度去冗余 + 核心帧→关键物体→证据"的 CoT 骨架可直接做 Understand Agent；"抽关键物体"天然对接 Generation Agent 的定位/裁剪。
- **指标**：20.3 万 QA 对；自有 bench 平均 34.1%，比 GPT-4o 高 7.7%，**只用同类 15% 算力**。
- **可复现**：✅ 开源，框架很轻(1B+0.5B+7B)。

#### 6. AKS / Adaptive Keyframe Sampling（CVPR 2025）
- arXiv 2502.21271｜GitHub ncTimTang/AKS
- **做了什么**：即插即用、**免训练**的关键帧选择模块。把选帧建成优化问题，同时最大化「问题相关性 s(Q,F)」+「时间覆盖度 c(I)」，用自适应递归二分(ADA)分配关键帧名额。打分用 BLIP/CLIP。
- **对我们的启发**：**最干净的"问题驱动选帧"积木**——比 VideoEspresso 的纯相似度去冗余更贴合"按用户文本指令找帧"。选出的 top 帧天然就是 Generation Agent 要定位/裁剪的候选图。
- **指标**：LongVideoBench 上 Qwen2-VL-7B 55.5%→60.5%(+5.0)。
- **可复现**：✅ 开源，免训练；显存要求 README 未给(主开销在基座 MLLM)。

#### 7. Grounded Video Caption Generation / GROVE（ICCV 2025）
- arXiv 2503.10781｜项目页 ekazakos.github.io/grounded_video_caption_generation
- **做了什么**：生成 caption 的同时把里面提到的物体用**时间上密集的框**框出来(grounding)。自动标注管线：GLaMM 帧级带框 caption → Llama-2 聚合成视频级 → 跨帧物体轨迹。**用词性标注抽「主-谓-宾(SVO)三元组」**喂 LLM 比喂原始 caption 更准。
- **对我们的启发**：**"SVO 结构化 + caption 名词↔视频框"正是 Understand→Generation 的桥**——Generation Agent 拿 SVO 里的"客体"直接去检测框/裁剪，对接"输出 Image"极顺；跨帧物体轨迹保证裁剪稳定。
- **指标**：HowToGround1M(100 万视频)；iGround 上 CIDEr 83.5、AP50 40.0。
- **可复现**：✅ 数据+代码+权重公开，但**百万级预训练成本高**（可直接用其权重做下游微调）。

#### 8. ScaleCap（**ICLR 2026** 接收）
- arXiv 2506.19848｜GitHub Cooperx521/ScaleCap
- **做了什么**：给模型更多"推理预算"换更全更准的图像描述。两招治幻觉：**启发式问答**(自动生成问题逐个回答补全细节) + **对比式句子打分**(对比"给图/不给图"说出某句的概率，差异小=凭语言习惯瞎编的幻觉，删掉)。**是串行迭代精炼，不是 N 次投票聚合**(常见误解，已纠正)。
- **对我们的启发**：这套"对比解码去幻觉"对 RAG 极值钱(防检索内容被脑补放大)；**把"推理时扩展(test-time scaling)"从图像迁移到"视频→图文"是空白卖点**。建议升级为**跨帧/跨模态一致性打分**(图文是否互相印证)做 RAG 事实性过滤闸。
- **指标**：标注 45 万图预训练，11 个 benchmark 一致提升；问题数 >20 后饱和。
- **可复现**：✅ 开源，推理 pipeline 轻量(7B 单卡)，代价是变慢。

#### 9. CUVA（CVPR 2024）— 视频异常的因果理解 Benchmark
- arXiv 2405.00181｜GitHub fesvhtr/CUVA
- **做了什么**：视频异常理解从"哪帧异常"升级到回答 **What(是什么)/Why(成因)/How(后果)**，提新指标 MMEval + 重要性曲线。
- **对我们的启发**：任务范式对口 Understand Agent 的结构化输出；**"重要性曲线"找关键片段可复用**；MMEval 的多维打分(尤其因果链合理性，BLEU/CIDEr 评不出来)可改造成我们的评测维度。异常视频→"标注关键帧图+因果解释文本"天然就是 Image+Text 输出。
- **指标**：1000 视频/32.46h/6000 QA；基线成因 58.92、后果 50.64(**普遍偏低=公认难点=我们的机会**)。
- **可复现**：✅ 数据+代码开源，规模适中可直接当评测集。

---

### C 组｜视频多智能体(Video-MAS) & 综述（看别人怎么分工，找差异化）

#### 10. MAGNET（NeurIPS 2025）
- arXiv 2506.07016｜GitHub schowdhury671/magnet
- **做了什么**：新任务"多视频大海捞针"——从一堆视频里检索多片段串起来回答。框架=AV-RAG 初检→动态实例化的音视频 agent 打分→**meta-agent 聚合**。
- **启发**："先 RAG 检索→多 agent 打分→meta-agent 聚合"可做 Understand Agent 骨架；但它的 agent 是**同质打分器、身份分工弱**——这正是我们做"身份明确、职责互补双 Agent"的差异化空间。
- **指标**：BLEU@4 最高 +89%。✅ 开源。

#### 11. LVAS-Agent（EMNLP 2025）
- arXiv 2503.10719｜项目页 lvas-agent.github.io
- **做了什么**：给长视频自动配音的"配音剧组"——4 角色(场景分割/剧本/音效/合成) + 讨论-纠错 + **生成-检索回路**。
- **启发**：**最贴合老师"身份+分工"理念**；它的"边生成边回检索库对齐"可直接借给 Generation Agent 出图前检索参考帧。它偏线性流水线→我们做 Understand↔Generation 双向反馈闭环来区分。
- **指标**：LVAS-Bench(207 视频)；量化值需查 PDF。✅ 有仓库。

#### 12. MovieAgent（ShowLab/NUS）
- arXiv 2503.07314｜GitHub showlab/MovieAgent
- **做了什么**：剧本梗概+角色库→多场景长片。多 LLM agent 扮演导演/编剧/分镜/场记 + 分层 CoT。**用 ED-LoRA 给每个角色训形象适配器保一致**。
- **启发**：直接命中老师"身份分工+LoRA/Adapter"；要保"视频里某物在生成图里一致"，LoRA/Adapter 是验证过的路子。它无"理解已有视频"端=我们的天然差异点。
- ✅ 推理代码开源，算力门槛中高。

#### 13. Mora（Lehigh）
- arXiv 2403.13248｜GitHub lichao-sun/Mora
- **做了什么**：用多个现成视觉 agent 拼起来复刻 Sora 的通用视频生成。
- **启发**：验证"把现成专长模型当 agent 串起来"工程可行；但纯线性管道、agent 间无身份博弈、效果仍逊 Sora→再次说明"线性管道"是普遍弱点。

#### 14. VideoAgent 研究线（综述，奠基作 ECCV 2024）
- 综述 emergentmind.com/topics/videoagent
- **整条线在做什么**：LLM(大脑/规划)+VLM(看图工具)，靠零样本工具调用迭代找关键帧做长视频 QA，**输出基本是文本**。常见角色：规划/感知/记忆 agent。奠基作 VideoAgent 在 EgoSchema 54.1%、平均只用 8.4 帧。
- **启发**：这条线**几乎全在"理解"端、输出文字**——把"理解端"和"图像生成端"在一个 MAS 里打通且做多模态 RAG，是明显空白。
- 附：CAMEL/AutoGen 是通用编排底座(管怎么对话分工)，不懂视频，可当我们两 Agent 的通信骨架。

---

### D 组｜工程基座 / 数据 / 工具 / 算力

- **基座 Qwen3-VL**(arXiv 2511.21631，已发布)：6 尺寸(2B/4B/8B/32B/30B-A3B/235B)×(Instruct快/Thinking推理强)；视频强(原生 256K 上下文、最多 2048 帧、秒级时间戳定位)。
  - **显存现实**：4090(24G) 上 BF16 LoRA 上限≈**4B**；要 8B 得 **QLoRA(4bit，约 24G 极限)** 或上 A100；32B 需 4bit 量化且紧张。A100-80G LoRA 微调 8B 实测占 40-45G。
- **微调框架**：**LLaMA-Factory**(有界面、上手最快、已支持 Qwen3-VL+视频) 或 **ms-swift**(阿里官方、跟新最快、有视频 LoRA 最佳实践)；底层都是 PEFT。务必"冻结视觉塔"省显存。
- **AVQA 数据集**（要"输出图"就看有没有 bbox 定位标注）：
  | 数据集 | 规模 | 公开 | bbox | 备注 |
  |---|---|---|---|---|
  | MUSIC-AVQA | 9.3K 视频/45.9K QA | ✅免申请 | ❌ | 最主流、baseline 多、起步首选 |
  | AVQA(清华 MM22) | 57K 视频 | ✅ | ❌ | 日常活动，注意≠MUSIC-AVQA |
  | Pano-AVQA | 5.4K 360°视频 | ⚠️待确认能否下载 | ✅球面 bbox | 唯一自带定位，但全景畸变 |
  | AVTrustBench/DAVE | — | ⚠️/✅ | ❌ | 评测基准非训练集，方向不对 |
- **RAG 基础设施**：**LightRAG**(港大，唯一原生多模态、增量强、便宜)做主系统；**HippoRAG2**(多跳最强)做对照。GraphRAG 建图太贵、无多模态，不建议。
- **视频内目标定位/裁剪**：**Grounding DINO**(开源、文本→框最强、4090 约 3-6G) 做主力；高吞吐加 YOLO-World；DINO-X 闭源付费仅做精度对照。
- **数据工具**：video-subtitle-extractor(提硬字幕→带时间戳 SRT，天然"视频↔台词"对齐)；DeepSeek-OCR(画面内复杂版面文字，单卡 20 万页/天)。→ 二者可**近零成本**把短剧/影视原片转成"视频+多源文本"语料。

---

## 二、提炼出的 3 个创新点（带论证）

> 综合 5 组调研，"现有视频系统普遍：①只在理解端、输出文字 ②生成端是线性剧组流水线 ③两端不共享 RAG ④基本没用 RL"。三个空白对应三个创新点，可组合也可单独立题：

### 创新点 1（架构层）：理解↔生成 双 Agent 闭环 + 双端共享多模态 RAG
- **内容**：Understand Agent(定位关键证据) 与 Generation Agent(据证据出图) **双向反馈**——出图员觉得证据不足就回退让理解员再定位；两个 Agent **共用同一个多模态 RAG 库**(既服务理解端检索片段，又服务生成端检索参考帧/风格)。
- **为什么新**：现有 MAS(MAGNET/Mora/LVAS/MovieAgent) 全是**单向线性流水线**；RAG 要么只用在理解端要么只用在生成端，**没人两端共享**。
- **风险/成本**：中。主要是工程编排，可借 ViMax 的质量门控 + AutoGen 骨架。

### 创新点 2（方法层）：Q-driven「关键证据定位→裁剪/生成」统一链路
- **内容**：把 **AKS 的"问题驱动选帧"** 从帧级细化到 **GROVE 的"SVO-客体级"**——用用户文本里的目标物体驱动「选帧 + 选框」，再接 **Video-RAG 的 APE/Grounding-DINO 检测**闭环到**裁剪/生成图像**；**选不到合适候选帧时退化为生成新图**(超越 M2RAG"只选不画")。
- **为什么新**：AKS 只选帧不落到物体；GROVE 能框物体但选帧不是问题驱动的；Video-RAG 有定位但只转文本没回用到产出——**把三者缝成一条"文本指令→关键帧→目标框→出图"的端到端可定位管线，正好填补各自缺口**，是最干净的一作主线。
- **风险/成本**：低-中。模块多为现成、免训练，4090 可跑。**建议作为首选主创新点。**

### 创新点 3（可靠性层）：推理时可扩展 + 跨模态一致性去幻觉的可信 RAG
- **内容**：把 **ScaleCap 的"对比解码去幻觉 + test-time scaling"** 从图像迁移到"视频→图文"，升级成**跨帧/跨模态一致性打分**(图与文是否互相印证)，作为 RAG 检索结果的**事实性过滤闸**；用 **CUVA 的因果维度(Why/How)** 做评测，攻"成因/后果理解是公认难点"这个缝。
- **为什么新**：test-time scaling 在视频→图文任务是空白；"图文互证"做 RAG 去幻觉相对原创。
- **风险/成本**：中。推理变慢，需设计一致性打分。可作为创新点 2 的"质量增强"叠加。

---

## 三、定题建议（3 个候选任务，按数据/算力可行性选）

老师给的 3 个候选：① 视频+文字→图+文字 ② 视频+文字→视频 ③ 视频浓缩。

**建议主攻 ①（视频+文字→图+文字），且"输出图"先做"检索/裁剪式"而非"生成式"。** 论证：
1. **Novelty 最稳**：唯一没被占的模态组合，差异化好讲。
2. **算力最省**：②视频生成要 Veo/Wan/HunyuanVideo，老师笔记自己也吐槽"人脸几个眼睛/不符合物理规律"+成本极高(50 元/条级)；①裁/截图几乎免费，4090 就够。
3. **Baseline 现成**：理解侧 VideoRAG + 输出侧 M2RAG，metric 脚手架直接复用。
4. **数据可低成本自造**：VSE+OCR 把短剧转成"视频+文字"，bbox 用 Grounding-DINO 伪标。

**③视频浓缩**作为备选(更安全但更拥挤、novelty 弱)；**②视频生成**不建议(最贵最难)。

---

## 四、下一堂课讨论提纲（建议主题）

**主题：从"文献调研完成"→"锁定创新点 + MVP 开工"**

1. **汇报**：文献调研已完成(本表，15+ 篇)，三大空白已定位。
2. **拍板任务定义**：确认主攻 ①视频+文字→图+文字、且"输出图=检索/裁剪式"(而非视频生成/浓缩)。
3. **确认创新点取舍**：3 个里以**创新点 2(Q-driven 定位链)为主线**，1(双 Agent 闭环)为架构外壳，3(去幻觉)为增强，是否认可这个组合？
4. **确认数据方案**：MUSIC-AVQA 起步 + Grounding-DINO 伪 bbox？还是争取 Pano-AVQA(需确认能否下载)？还是自建短剧数据(VSE+OCR)？
5. **确认 baseline 与基座**：理解侧 VideoRAG + 输出侧 M2RAG 双 baseline；基座 Qwen3-VL-4B(4090) 起步还是直接 8B(用学校 A100)？
6. **下一步任务**：MVP = 4B + MUSIC-AVQA + LoRA 先把"视频+文字→文字"理解链跑通，再加图像输出。

---

## 五、要请老师确认的问题清单

1. **目标会场最终定哪个？** 12-07 读的论文全是 CVPR/ICCV/ACMMM(像在往 CV 顶会靠)，但老师最初提的 ACL ARR 2 月 ddl 很紧。CV 顶会 vs SCI 一区(KBS/ESWA) vs ACL，写作风格和实验量差很多。
2. **"输出 Image"到底指**"从视频里裁/检索关键帧"(便宜) 还是 "生成新图"(贵)？这是工作量分水岭。
3. **RL 范围**：Agent-Token-RL / MARL 是否必须？(调研结论：多智能体 RL 训练不稳定、算力吃不消，建议先 SFT/LoRA，RL 留展望章节。)
4. **数据**：接受用 Grounding-DINO 给 MUSIC-AVQA 补伪 bbox 吗？还是必须用自带 bbox 的数据？
5. **基座尺寸**：认可 4090 上先用 4B、大实验攒批上学校 A100 跑 8B 的省钱策略吗？
6. **baseline 认可**：理解侧 VideoRAG + 输出侧 M2RAG 作双主 baseline，可以吗？

---

## 六、我的额外建议

- **时间最紧的两件事先做**：辅导课快到期，优先推进"文献调研交付(本表已成)"+"定题+MVP"，让课程能立刻往前走。
- **MVP 配置(最省钱)**：Qwen3-VL-4B-Instruct + MUSIC-AVQA + LLaMA-Factory/ms-swift LoRA，全程 4090(约 2 元/h)；只把必须大显存的实验攒批放学校 100h A100。
- **数据先行**：用 video-subtitle-extractor + DeepSeek-OCR 搭一条"短剧→视频+文字"的自动管线，既解决 image-output 缺 bbox 的问题，也能做出"自建数据集"这个加分项。
- **引用纠错**：老师笔记里 M2RAG 的 arXiv 号(2507.09876)写错了，真实是 **2411.16365**；ScaleCap 是"迭代精炼"不是"N 次投票"——写论文/讨论时注意。
- **待核实清单**：VideoRAG 是否真 KDD'26、M2RAG 会场、ViMax 接收情况、Pano-AVQA 能否下载、各模型含视频的真实显存——这些建议在投稿前实测/核对。

---

## 附：本次自动完成的工作
- 从旧 Windows(经 Tailscale SSH)取回并通读了全部 4 次飞书辅导笔记(2025-10-10 / 11-02 / 11-17 / 12-07)，已归档于 `00_老师辅导笔记/`。
- 5 个联网调研 agent 并行精读了 15+ 篇论文/项目 + 工程栈，产出本表。
