# 文献 / 数据 / 生成路线 更新（2026-06-26）

> 起因：老师 2026-06-26 反馈"①文献调研要更新 ②测评数据集要更新；方法思路暂时没问题"，并把"输出图"定为**基于关键帧生成新图**（非裁剪）。
> 本文档汇总三路联网调研结果，是对 `docs_文献调研与进展.md` 的增补。对应 issue #8 / #9 / #3。

---

## 一、最新相关工作 & novelty 现状（对应 #8）

### 结论：题目 novelty 仍成立，但边缘在被快速包围，需更锋利的定义 + 自建基准
把任务拆成 4 个特征——(a) 视频输入、(b) 输出**新生成**的图、(c) 图文交错、(d) MAS + 多模态 RAG——**目前没有任何一篇同时占齐**：
- 占了 (b)(c)+检索增强生成图：**ORIG**(2510.22521)、**Gen-Searcher**(2603.28767)、**ImageRAG**(2502.09411) —— 全是**文本输入、无视频**。
- 占了 (a)+视频 RAG：**VimRAG**(2602.12735)、**Multi-RAG**(2505.23990)、VideoRAG —— **只检索/答文，不生成新图**。
- 占了 (c)+agentic 产图：**ATP-Bench**(2603.29902)、**M2IO-R1**(2508.06328)、**MRAMG-Bench**(2502.04176) —— **无视频、图多为检索挑选**。

→ **"视频驱动 + 关键帧 grounding 生成新图 + 多智能体"这个交点仍是空位。**

### ⚠️ 风险（必须正视）
单体统一大模型（**BAGEL** 2505.14683 / **Show-o2** 2506.15564 / **Mogao** 2505.05472）+ 现成 video-RAG，理论上能"端到端"拼出近似系统。
**应对**：尽早把任务**形式化并出一个专属小基准**（对标 MRAMG-Bench/FIG-Eval，但带**视频输入 + 关键帧一致性**评测），用"可控、可溯源、关键帧一致"论证 MAS+RAG 相比单体模型不可替代。**且要抓紧**，拖到 2026 下半年易被覆盖。

### related work 必须新增（按威胁度）
1. **M2IO-R1**(2508.06328)、**MRAMG-Bench**(2502.04176) — 图文交错输出的最新方法/基准
2. **ATP-Bench**(2603.29902) — agentic 交错生成，最逼近 MAS
3. **ORIG**(2510.22521)、**Gen-Searcher**(2603.28767)、**ImageRAG**(2502.09411) — "检索增强生成新图"三篇正面区分
4. **VimRAG**(2602.12735)、**Multi-RAG**(2505.23990) — 视频侧多模态 RAG
5. 统一模型背景：**BAGEL** / **Show-o2** / **Mogao**
6. 可借鉴 grounding 叙事：**Video Evidence to Reasoning**(2601.07761) — 时间锚点把结论绑到具体帧

---

## 二、数据集 & baseline 更新（对应 #9）

### 主数据集：换 **V-STaR** 替代 MUSIC-AVQA
- V-STaR（2025.03，arXiv 2503.11495）：2,094 视频 / 16,793 **bbox** / when-where-what 三段 CoT；**HF+GitHub 全公开**（29.6GB）。**自带定位标注**，正合"输出图"需要。
- 补充：**Open-o3-Video / STGR**（2510.20579）——关键帧 + 框 + 时间戳 + CoT，是"基于关键帧生成"的**强监督信号**（⚠️数据/代码 release 待项目页确认）。
- 缺框数据兜底：**Grounding-DINO** 按问题实体名跑伪框 + 抽检（打 `pseudo=true`）。

### 新 baseline
- **主 baseline = Open-o3-Video**（2025，Qwen2.5-VL-7B；视频→关键帧+框+文字 CoT）——输入输出与我们主任务最一致。
- **图文交错对照 = LLM-I**（2509.13642，把交错生成当 tool-use）。
- **M2RAG 升级版 = M2IO-R1**（2508.06328，RL 增强、可训练）接替原 M2RAG。
- VideoRAG 仍作家族对照。

---

## 三、Generation Agent 技术栈（对应 #3，Q4=生成）

> 现状：**没有**"视频关键帧→图"专用模型。务实做法 = 抽关键帧+目标物体 → 把关键帧当**参考图/条件** → 喂图像**生成或编辑**大模型 → 出新图。

### 推荐技术栈
```
视频 ─(关键帧抽取: 见 #2 Q-driven选帧 + 目标物体框/SAM分割)─► 关键帧 + 目标主体
   ├─[A 生成线·主体保持] FLUX.1-dev(12B) + OminiControl(subject)  (+ControlNet保构图; 人物叠 PuLID/InfiniteYou)
   │     └► 让"视频里那个物体/人"出现在新场景的新图
   └─[B 编辑线·指令重绘] FLUX.1 Kontext-dev  或  Qwen-Image-Edit-2509(Apache2.0/多图/中文)
         └► 以关键帧为底 + 文字指令 → 改/加内容出新图
评测: VQAScore(指令一致) + DreamSim/DINO(主体一致) + FID(画质) + 人工胜率
```
- **主基座**：FLUX.1-dev（生态/配件最全；4090 量化可推理、可训 LoRA；大实验上 A100）。许可为科研非商用，做论文可用。
- **主条件控制**：**OminiControl**（ICCV2025，DiT 统一控制器，+0.1% 参数，subject/spatial 双模式，最贴"以关键帧主体为条件生成"）。
- **可商用替代**（避许可顾虑）：Z-Image-Turbo(6B,Apache2.0,~16GB) / Qwen-Image-Edit-2509(Apache2.0) / FLUX.2 klein-4B。

### 最省钱 MVP 生成方案（4090 单卡、可零训练）
1. 抽帧（CLIP/VLM 选帧 + 目标框，SAM 可选）——零训练
2. 生图二选一：**FLUX.1 Kontext-dev**（FP8/量化 ~12–20GB，"关键帧+文字指令→新图"**免微调**）；或省显存可商用走 **Z-Image-Turbo**
3. 需强主体保持时挂 **OminiControl subject**（先用官方权重免训练；不够再 4090 训控制分支，显存友好）
4. 评测：跑 **VQAScore + DreamSim** 出量化数 + 小规模人工 A/B

### 4090 LoRA 可行性
FLUX.1-dev LoRA：开梯度检查点+512²+batch1+梯度累积+BF16+8bit优化器，可压到 16–18GB，4090 可训（约 SDXL 2 倍耗时；工具 Kohya/FluxGym/ai-toolkit）。全量/FP16 大实验上 A100。

---

## 四、对路线图的影响（要改的地方）
- **#3（M2 决策闭环）**：从"裁剪优先、生成兜底" → 改为 **以关键帧为条件的生成为主**（见 `创新点2_方法论框架.md` §7 更新）。
- **#6（评测）**：图像端指标改为 **VQAScore + DreamSim + FID + 人工胜率**。
- **#7（数据管线）**：主数据换 V-STaR（自带 bbox），schema 统一为 `{video, query, answer_text, keyframe_ts, bbox}`，无框来源加 Grounding-DINO 伪标节点。
- **新增动作**：尽快**形式化任务 + 出专属小基准**（带视频输入 + 关键帧一致性），抢时间窗。

> 待核实：Open-o3-Video/Video-CoT 的数据/代码 release；FLUX.2 klein 子型号显存；Qwen-Image-Edit-2511/2512 增量；部分 2026 预印本入稿前逐篇复核。
